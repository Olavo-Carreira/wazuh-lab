<!--
  Wazuh Lab — custom rules (student-facing)

  IDs 100100–100179. Each rule is described in terms of what it
  *literally matches* — a regex, a field value, a decoded substring.
  The descriptions do not name the attack class each rule represents;
  mapping them to CVEs and MITRE techniques is part of Lab 3.

  Severity levels are a starting point. Several are deliberately
  conservative; raising them (and adding correlation rules) is part of
  Lab 3.
-->

<group name="wazuh-lab,">

  <rule id="100099" level="0">
    <if_sid>19009</if_sid>
    <description>Suppress CIS benchmark baseline noise</description>
  </rule>

  <rule id="100120" level="12">
    <if_sid>31100</if_sid>
    <regex>\.%2e/|/%2e\.</regex>
    <description>Path Traversal: encoded dot-dot segment in URL (CVE-2021-41773)</description>
    <mitre>
      <id>T1190</id>
    </mitre>
    <group>attack,exploit,web,</group>
  </rule>

  <rule id="100175" level="7">
    <if_sid>31105</if_sid>
    <description>HTTP request: url contains a scripting expression pattern</description>
    <mitre>
      <id>T1059.007</id>
    </mitre>
  </rule>

  <rule id="100130" level="13">
    <match>jndi:</match>
    <description>Log4Shell: JNDI injection attempt detected (CVE-2021-44228)</description>
    <mitre>
      <id>T1190</id>
    </mitre>
    <group>attack,exploit,</group>
  </rule>

  <rule id="100140" level="12">
    <match>pkexec</match>
    <description>PwnKit: pkexec execution observed - possible privilege escalation (CVE-2021-4034)</description>
    <mitre>
      <id>T1068</id>
    </mitre>
    <group>attack,exploit,privilege_escalation,</group>
  </rule>

  <rule id="100150" level="15">
    <if_sid>550,554</if_sid>
    <field name="file">/etc/passwd</field>
    <description>Privilege Escalation: /etc/passwd modified - possible backdoor user added (CVE-2021-4034)</description>
    <mitre>
      <id>T1068</id>
      <id>T1136</id>
    </mitre>
    <group>attack,exploit,privilege_escalation,</group>
  </rule>

  <rule id="100165" level="3">
    <decoded_as>corpnet-login</decoded_as>
    <match>result=failure</match>
    <description>CorpNet Portal: login failed for user $(dstuser)</description>
  </rule>

  <rule id="100166" level="5">
    <decoded_as>corpnet-login</decoded_as>
    <match>result=success</match>
    <description>CorpNet Portal: login succeeded for user $(dstuser)</description>
  </rule>

  <rule id="100163" level="3">
    <if_sid>31101</if_sid>
    <url>/login</url>
    <description>HTTP: POST /login — authentication failed</description>
  </rule>

  <rule id="100164" level="5">
    <if_sid>31108</if_sid>
    <match>POST /login</match>
    <description>HTTP: POST /login — authentication successful</description>
  </rule>

  <rule id="100162" level="4">
    <if_sid>31108</if_sid>
    <match>POST /reports/submit/</match>
    <description>Apache: POST request to /reports/submit/ — report submitted</description>
  </rule>

  <rule id="100160" level="3">
    <if_sid>554</if_sid>
    <field name="file">/var/lib/reports/</field>
    <description>FIM: new file under /var/lib/reports/</description>
  </rule>

  <rule id="100170" level="10">
    <match>tar</match>
    <description>Exfiltration: tar invoked - possible data collection or exfiltration</description>
    <mitre>
      <id>T1560</id>
    </mitre>
    <group>attack,exfiltration,</group>
  </rule>

  <rule id="100299" level="0">
    <if_sid>19007,19008,19009,510,501</if_sid>
    <description>Suppress baseline noise</description>
  </rule>

  <rule id="100131" level="15" timeframe="120">
    <if_matched_sid>100130</if_matched_sid>
    <if_sid>100150</if_sid>
    <description>CRITICAL: Log4Shell RCE confirmed - JNDI injection followed by /etc/passwd modification</description>
    <mitre>
      <id>T1190</id>
      <id>T1068</id>
      <id>T1136</id>
    </mitre>
    <group>attack,exploit,privilege_escalation,critical,</group>
  </rule>

</group>
